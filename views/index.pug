extends layout

block content
  h1= title
  p Hello, welcome to #{title}!
  button(type="button", class="btn btn-secondary", onclick="double()") Press me
  div#double Up the amount.

  script.
      let value = 0;
      let power = 1;
      
      function double() {
        value += 1;
        power *= 2;
        document.getElementById('double').innerText = `Two to the power ${value} is ${power}`;
      }
